def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist

def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist
def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist


def duplication(text):
    alist =""
    for i in text:
        alist += i
        alist += i
    return alist
        

def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist

def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist
def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist


def duplication(text):
    alist =""
    for i in text:
        alist += i
        alist += i
    return alist
        

def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist

def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist
def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist


def duplication(text):
    alist =""
    for i in text:
        alist += i
        alist += i
    return alist
        

def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist

def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist
def duplication(text):
    alist = ""
    for i in text:
        alist += str(i)
        alist += str(i)
    return alist


def duplication(text):
    alist =""
    for i in text:
        alist += i
        alist += i
    return alist
        

