def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
def censorship(text):
    for i in range(len(text)):
        if i%2 == 1:
            text = text[:i] + 'X' + text[i+1:]
    return text

def censorship(text):
    for i in range(0, len(text)):
        if i % 2 != 0:
            text = text[:i] + "X" + text[i+1:]
    return text

def censorship(text):

    for i in range(len(text)):
        if i % 2 != 0:
            text = text[:i] + 'X' + text[i+1:]

    return text
